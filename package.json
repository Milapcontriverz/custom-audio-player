{
  "name": "capacitor-audio-player-demo",
  "version": "1.0.13",
  "description": "Capacitor Audio Player Plugin",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "prepare": "npm run build"
  },
  "keywords": ["capacitor", "plugin", "audio"],
  "capacitor": {
    "ios": {
      "src": "ios"
    },
    "android": {
      "src": "android"
    }
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0",
    "typescript": "^4.9.5",
    "rimraf": "^3.0.2"
  },
  "peerDependencies": {
    "@capacitor/core": "^7.0.0"
  }
}